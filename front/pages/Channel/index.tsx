import React from 'react';
import Workspace from '@layouts/Workspace';

const Channel = () => {
  return (
    <Workspace>
      <div> 로그인성공(채널)</div>
    </Workspace>
  );
};

export default Channel;
